{
	"name": "R Development Environment",
	"image": "rocker/r-ver:4.3",

	"features": {
		"ghcr.io/rocker-org/devcontainer-features/r-packages:1": {
			"packages": "tidyverse,rmarkdown,knitr,ggplot2,dplyr,readr,corrplot,car,scales,gridExtra,tinytex"
		}
	},

	"customizations": {
		"vscode": {
			"extensions": [
				"REditorSupport.r",
				"rdebugger.r-debugger",
				"quarto.quarto"
			],
			"settings": {
				"r.rterm.linux": "/usr/local/bin/R",
				"r.bracketedPaste": true,
				"r.plot.useHttpgd": true
			}
		}
	},

	"postCreateCommand": "apt-get update && apt-get install -y pandoc perl-modules-5.34 && R -e \"install.packages(c('tidyverse', 'rmarkdown', 'knitr', 'ggplot2', 'dplyr', 'readr', 'corrplot', 'car', 'scales', 'gridExtra', 'tinytex'), repos='https://cloud.r-project.org/'); tinytex::install_tinytex()\"",

	"remoteUser": "root"
}
